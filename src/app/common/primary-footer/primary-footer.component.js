import template from './primary-footer.html';
import controller from './primary-footer.controller';
import './primary-footer.scss';

let primaryFooterComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default primaryFooterComponent;
